---
title: Vanilla
description: "Jum is a library that enables natural image zoom in/out gestures."
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs'

## Installation
<Tabs items={['npm', 'pnpm', 'yarn']} groupId="language">
  <Tab value="npm">
    ```bash
    npm i jum-core
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add jum-core
    ```  
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add jum-core
    ```
  </Tab>
</Tabs>